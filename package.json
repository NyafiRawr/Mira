{
  "name": "mira",
  "version": "20.2.16",
  "description": "Discord-развлекательный бот с командами osu!",
  "license": "GPL-2.0",
  "author": {
    "name": "NyafiRawr",
    "tag": "NyafiRawr#7358",
    "id": "197999736035344384"
  },
  "contributors": [
    "deissh",
    "Fataliti // Reifshneider"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/NyafiRawr/Mira"
  },
  "main": "src/index.ts",
  "typings": "lib/index.d.ts",
  "scripts": {
    "start": "ts-node . | pino-pretty",
    "start:dev": "nodemon --watch 'src/**/*.ts' --ext 'ts' --exec 'node' -r ts-node/register --inspect=9229 . | pino-pretty",
    "start:prod": "npm run build && node ./lib/index.js | pino-tee error ./logs/error.log",
    "clean": "rimraf lib && rimraf coverage",
    "format": "prettier --write \"{src,__tests__}/**/*.ts\" --single-quote --trailing-comma es5",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean && npm run format && npm run lint && echo Using TypeScript && tsc --version",
    "build": "tsc --pretty",
    "coverage": "jest --forceExit --coverage",
    "watch": "npm run build -- --watch",
    "release:major": "npm version major && git push origin && git push origin --tags",
    "release:minor": "npm version minor && git push origin && git push origin --tags",
    "release:patch": "npm version patch && git push origin && git push origin --tags"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "@types/bluebird": "^3.5.29",
    "@types/validator": "^12.0.1",
    "axios": "^0.19.2",
    "discord.js": "^11.5.1",
    "dotenv": "^8.2.0",
    "keyv": "^4.0.0",
    "moment": "^2.24.0",
    "mysql2": "^2.0.2",
    "pino": "^5.16.0",
    "pino-pretty": "^3.5.0",
    "pino-tee": "^0.3.0",
    "sequelize": "^5.21.4",
    "sqlite3": "^4.1.1",
    "typescript": "^3.7.5"
  },
  "devDependencies": {
    "@types/keyv": "^3.1.1",
    "@types/node": "^13.7.1",
    "@types/pino": "^5.15.1",
    "nodemon": "^2.0.2",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.6.2",
    "tslint": "^6.0.0",
    "tslint-config-prettier": "^1.18.0"
  }
}
